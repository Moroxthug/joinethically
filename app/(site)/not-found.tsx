import NotFoundBody from "@/components/NotFoundBody";

/** notFound() from any page in the site group, rendered inside header/footer. */
export default function SiteNotFound() {
  return <NotFoundBody />;
}
