import NotFoundBody from "@/components/NotFoundBody";

/** Unmatched URLs outside the site route group — no header/footer available here. */
export default function NotFound() {
  return <NotFoundBody />;
}
